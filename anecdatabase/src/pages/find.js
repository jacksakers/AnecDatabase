/* Find Page
Users use the Find page to discover and search for events. The main feature
is the search bar. The website renders tiles for events found matching the
search query, which can be clicked to go to that event's Event Page */

import { Component } from "react";

// Rendering
class Find extends Component {
    constructor(props) {
      super(props);
      this.state = {
    };
  }

  render() {
    return (<div>

    </div>
    );
  }
}


export default Find;